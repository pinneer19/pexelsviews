<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PexelsViews" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
<!--        <item name="colorPrimary">@color/white</item>-->
    </style>

    <style name="Theme.PexelsViews" parent="Base.Theme.PexelsViews" />
    <style name="Theme.PexelsViews.Splash" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.PexelsViews</item>
        <item name="windowSplashScreenBackground">@color/red</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_animation</item>
    </style>
</resources>